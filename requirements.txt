# Core
streamlit==1.38.0

# Deep learning — CPU build, since Community Cloud has no GPU.
# (Locally you can keep your CUDA build; this file only controls
# what gets installed on the deployed server.)
torch==2.4.1
torchvision==0.19.1

# Image / array processing
numpy==1.26.4
opencv-python-headless==4.10.0.84
Pillow==10.4.0

# Satellite data
earthengine-api==0.1.412
geemap==0.34.5
rasterio==1.3.10

# Map UI
folium==0.17.0
streamlit-folium==0.23.1

# Charts
matplotlib==3.9.2
